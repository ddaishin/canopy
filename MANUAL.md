# Canopy 操作マニュアル

> 対象バージョン: v0.2.0  
> 最終更新: 2026-04-13

---

## 1. はじめに

Canopy は、Claude Code CLI セッションを管理するデスクトップアプリです。プロジェクトフォルダを登録し、Claude Code セッションやシェルターミナルをタブで起動・管理できます。すべてローカルで動作し、外部 API への依存はありません。

### 前提条件

| ツール | 用途 | インストール |
|--------|------|-------------|
| Claude Code CLI | AI セッション | `npm install -g @anthropic-ai/claude-code` |
| GitHub CLI (gh) | GitHub ダッシュボード | https://cli.github.com/ |
| Node.js v18+ | フロントエンドビルド | https://nodejs.org/ |

---

## 2. インストール

### Windows

リリースページから `.msi` または `.exe` をダウンロードしてインストールします。

ソースからビルドする場合:

```bash
git clone https://github.com/The-Banana-Standard/canopy.git
cd canopy
npm install
npm run tauri build -- --bundles msi
```

出力先: `src-tauri/target/release/bundle/msi/Canopy_x.x.x_x64_en-US.msi`

### macOS / Linux

```bash
curl -fsSL https://raw.githubusercontent.com/The-Banana-Standard/canopy/main/install.sh | sh
```

---

## 3. 画面構成

アプリを起動すると、以下の 4 つのエリアで構成された画面が表示されます。

```
+--------------------------------------------------+
|  タイトルバー                      [_ ▢ X]       |
+------+------------------+------------------------+
|      |  タブバー        |                        |
| サイ |------------------| アクティビティ          |
| ド   |                  | バー                   |
| バー |  メインコンテンツ |（右端、折りたたみ可）  |
|      |                  |                        |
+------+------------------+------------------------+
```

| エリア | 役割 |
|--------|------|
| タイトルバー | ホームアイコン（ワークスペース概要に戻る）、ウィンドウ操作 |
| サイドバー（左） | ワークスペース・プロジェクト一覧、追加・削除操作 |
| メインコンテンツ | ホーム画面、ターミナル、プロジェクト概要をタブで切り替え |
| アクティビティバー（右） | 使用量統計、Skills、設定などのパネルを展開 |

---

## 4. 開発の流れ

以下では、日常的な開発作業を Canopy でどのように行うかを順を追って説明します。

### 4.1 ワークスペースとプロジェクトの登録

**初回起動時に行う設定です。**

1. サイドバーの「**Add Workspace**」をクリック
2. フォルダ選択ダイアログで、プロジェクト群を含む親フォルダを選択
3. サブディレクトリが自動スキャンされ、プロジェクトとして一覧に表示される

個別のプロジェクトだけ追加したい場合は「**Add Project**」を使います。

> **ヒント:** ワークスペースを削除してもディスク上のファイルは消えません。Canopy の管理対象から外れるだけです。

### 4.2 ホーム画面で状況を把握する

サイドバーで何も選択していない状態、またはタイトルバーのホームアイコンをクリックすると、**ワークスペース概要**（ホーム画面）が表示されます。

ホーム画面には以下の情報が集約されています:

| セクション | 内容 |
|-----------|------|
| Quick Stats | プロジェクト数、セッション数、メッセージ数、トークン消費量 |
| Daily Planner | 今日のタスク一覧。追加・完了・Claude への送信が可能 |
| Recent Projects | 最近作業したプロジェクトのカード（ブランチ名、最終コミット） |
| GitHub | 自分に関連する PR と Issue の一覧 |
| Skills Store | インストール済みスキルのクイックアクセス |

### 4.3 タスクを計画する（Daily Planner）

1. ホーム画面の Daily Planner セクションで、テキスト入力欄にタスクを入力し Enter
2. ドロップダウンでプロジェクトを割り当て
3. タスクにカーソルを合わせると「**Send to Claude**」ボタンが表示される
4. クリックすると、そのタスクをコンテキストとした Claude セッションが新しいタブで開く

完了したタスクはチェックボックスをクリック。「**Clear completed**」で一括削除できます。

### 4.4 Claude Code セッションを起動する

**方法 1: キーボードショートカット**

- `Cmd+T`（macOS）/ `Ctrl+T`（Windows）で新しい Claude セッションを開く

**方法 2: プロジェクトから起動**

1. サイドバーでプロジェクトをクリック → プロジェクト概要タブが開く
2. 「**New Claude Session**」ボタンをクリック

**方法 3: Quick Actions（タブバー右クリック）**

タブバーを右クリックすると、以下のクイックアクションが表示されます:

| アクション | 説明 |
|-----------|------|
| Review PR | PR レビュー用のプロンプトで Claude を起動 |
| Fix Tests | テスト修正用のプロンプトで Claude を起動 |
| Commit | コミット作成用のプロンプトで Claude を起動 |
| Plan Feature | 機能設計用のプロンプトで Claude を起動 |
| Audit Code | コード監査用のプロンプトで Claude を起動 |
| Simplify | コード簡素化用のプロンプトで Claude を起動 |

### 4.5 シェルターミナルを使う

`Cmd+Shift+T` / `Ctrl+Shift+T` で通常のシェルターミナルを開けます。git 操作やビルドコマンドの実行など、Claude を介さない作業に使います。

> **ヒント:** ファイルやフォルダをターミナルにドラッグ&ドロップすると、パスが自動で入力されます。

### 4.6 複数ターミナルを並べて作業する（Split Mode）

タブバーのグリッドアイコンをクリックすると **Split Mode** に切り替わります。開いているターミナルタブがグリッド状に並べて表示されます。

- 2 タブ: 左右 2 カラム
- 3 タブ以上: 自動でグリッド配置

Claude セッションでコードを書かせながら、隣のシェルでテストを実行する、といった使い方ができます。

### 4.7 Workspace Agent を使う

ホーム画面の「**Workspace Agent**」ボタンをクリックすると、ワークスペース内の全プロジェクトのコンテキストを持った Claude セッションが起動します。

プロジェクト横断の質問（「どのプロジェクトで認証ライブラリを使っている？」など）に有効です。

### 4.8 GitHub の PR / Issue を確認する

GitHub CLI (`gh`) がインストール・認証済みであれば、ホーム画面の GitHub セクションに以下が自動表示されます:

- 自分が作成した PR
- レビューをリクエストされた PR
- アサインされた PR
- アサインされた Issue

PR や Issue をクリックすると、関連プロジェクトで Claude セッションを開いて作業を開始できます。

### 4.9 セッション履歴から再開する

過去の Claude セッションはプロジェクト概要タブに一覧表示されます。

1. サイドバーでプロジェクトをクリック
2. Sessions List から再開したいセッションの「**Resume**」をクリック
3. 前回の続きから Claude との対話を再開

### 4.10 Skills を活用する

Skills は Claude Code のスラッシュコマンドを拡張する仕組みです。

**インストール:**

1. アクティビティバーの星アイコンをクリック
2. 「Browse」タブでカテゴリ（Documents, Development, Design 等）から検索
3. 「**Install**」をクリック

**実行:**

インストール済みスキルをクリックすると、`/<スキル名>` プロンプト付きで Claude セッションが起動します。

---

## 5. キーボードショートカット一覧

| ショートカット | 操作 |
|---------------|------|
| `Cmd/Ctrl + T` | 新しい Claude セッションを開く |
| `Cmd/Ctrl + Shift + T` | 新しいシェルターミナルを開く |
| `Cmd/Ctrl + W` | アクティブなタブを閉じる |
| `Cmd/Ctrl + 1` | ホームタブに移動 |
| `Cmd/Ctrl + 2〜9` | 指定番号のタブに移動 |
| `Cmd/Ctrl + Shift + [` | 前のタブに移動 |
| `Cmd/Ctrl + Shift + ]` | 次のタブに移動 |
| `Cmd/Ctrl + F` | ターミナル内検索 |

---

## 6. 設定

アクティビティバー最下部の歯車アイコンから設定パネルを開きます。

### Claude プロバイダー設定

| プロバイダー | 必要な設定 |
|-------------|-----------|
| Direct | `ANTHROPIC_API_KEY` 環境変数 |
| AWS Bedrock | Region, Access Key ID, Secret Access Key |
| Google Vertex | Project ID, Credentials JSON |

設定変更後「**Save**」をクリックして保存します。

---

## 7. トラブルシューティング

| 症状 | 対処 |
|------|------|
| Claude セッションが起動しない | Claude Code CLI がインストールされているか確認: `claude --version` |
| GitHub ダッシュボードが空 | `gh auth status` で認証状態を確認 |
| macOS で「Canopy is damaged」と表示 | `xattr -cr /Applications/Canopy.app` を実行 |
| タブの状態が復元されない | アプリを正常終了（ウィンドウの X ボタン）で閉じる |
